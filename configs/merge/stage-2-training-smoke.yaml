{
  "method": "lora",
  "epochs": 1,
  "batch_size": 1,
  "learning_rate": 1e-4,
  "warmup_steps": 10,
  "max_steps": 5000,
  "gradient_accumulation_steps": 4,
  "lora_rank": 16,
  "lora_alpha": 16,
  "lora_target_modules": ["to_q", "to_k", "to_v", "to_out.0", "ff.net.0.proj", "ff.net.2"],
  "save_steps": 100,
  "eval_steps": 50,
  "logging_steps": 10,
  "resolution": 512,
  "mixed_precision": "bf16",
  "gradient_checkpointing": true,
  "seed": 2025,
  "smoke": true,
  "smoke_steps": 50,
  "dataset_config": "configs/merge/stage-2-synthetic-dataset.yaml",
  "_note": "Smoke config: 50 steps at 512px, 16 prompts only. Validates LoRA injection, forward pass, backward pass, checkpoint save. If this passes cleanly, full run is ~20h at max_steps=5000."
}
