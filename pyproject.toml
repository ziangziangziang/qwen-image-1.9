[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qwen-image-1-9"
version = "0.2.0"
description = "Three-step Qwen-Image 1.9 checkpoint pipeline with shared results reporting."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
dependencies = [
  "accelerate>=0.30",
  "diffusers>=0.30",
  "matplotlib>=3.8",
  "Pillow>=10.0",
  "rich>=13.7",
  "safetensors>=0.4",
  "torch>=2.2",
  "tqdm>=4.66",
  "transformers>=4.44"
]
authors = [
  { name = "Qwen-Image 1.9 Community Bridge" }
]
keywords = ["qwen", "image-generation", "remote-first", "research-scaffold"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11"
]

[project.scripts]
q19 = "qwen_image_19.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
